name: in_app_navigator
description: "A new Flutter project."
version: 1.0.0
repository: https://github.com/Mohiuddin655-PUB/flutter_in_app_navigator.git

environment:
  sdk: '>=3.4.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter: